CC=gcc
CFLAGS= -pthread
OBJECTS= main.o reader.o list.o llist.o

#-Wall -lpthread
# -ansi

.PHONY: clean all

all: $(OBJECTS)
	gcc ${CFLAGS}  $(OBJECTS) -o main.a

#main.o: main.c 
#	gcc ${CFLAGS} -c main.c -o main.o
#
#list.o: list.c
#	gcc ${CFLAGS} -c list.c -o list.o

%.o: %.c
	gcc -c  -o $@ $< ${CFLAGS} 

#reader.o: reader.c
#	gcc ${CFLAGS} -c list.c -o list.o
#
#LList.o: LList.c
#	gcc ${CFLAGS} -c LList.c -o LList.o
#
clean:
	rm *.o *.a *.stackdump *.exe waitn wsh

